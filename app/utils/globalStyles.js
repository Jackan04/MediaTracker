import { StyleSheet } from "react-native"
import {COLORS, FONT_SIZES, SIZES, FONTS} from '../utils/theme.js'


const globalStyles = StyleSheet.create({
    
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    }
})
    
